# Archived Checkpoint 03C.4 branch material

This directory preserves the unique files from the historical branch
`checkpoint-03c4-nested-stacking` before branch cleanup.

The files are stored under their original relative paths beneath this archive
directory. They are **not active project code** and are intentionally excluded
from the current package/test workflow.

Canonical interpretation:
`docs/CHECKPOINT_03C4_FINDINGS.md`.

Original branch tip:
`d0510a740342d9b8ec7deb3e75eaa4aa3ab4d27d`.

The branch history is also merged into `main` ancestry using an ours-style
history merge, so no historical commit is lost when the branch reference is
deleted.
