# AI Handoff — GeoCebada

**Updated:** 2026-09-18  
**Current phase:** Checkpoint 02 is closed. Checkpoint 03 — Modeling is next.

Read `docs/AGENT_GUIDE.md` first, then `docs/PROJECT_HISTORY.md` for chronology. This file is
only a compact operational summary.

## Mission

Predict barley parcel yield `RENDIMIENTO_T_HA` for the 59 hidden-target parcels using a
reproducible combination of remote sensing, climate, soil, topography and spatial/admin
information.

Canonical contract:

```text
ID_POLIGONO
RENDIMIENTO_T_HA
CONJUNTO
197 parcels = 138 ENTRENAMIENTO + 59 PREDICCION
target cycle = April–October 2025
states = Hidalgo, Puebla, Tlaxcala
```

Never reconstruct or use the 59 hidden targets.

## Current reproducible state

Completed milestones:

- Checkpoint 01 — data acquisition/audit/integration fixtures: closed.
- Data Contract v2: PASS (46 pass, 0 warn, 0 fail, 1 intentional skip).
- Feature Table v1: built and independently validated.
- Checkpoint 02 — feature inventory, train/prediction diagnostics, frozen CV, ablations,
  untuned baselines: closed.
- Canonical Feature Table v1 artifacts are committed to Git.

Canonical model-ready inputs:

```text
data/processed/features_v1/
  parcel_features_clean.csv       197 × 699
  parcel_features_competition.csv 197 × 1408
  parcel_features_all.csv         197 × 1408
  feature_manifest.json
  build_report.json
```

Manifest counts:

```text
clean features             694
competition-only features  709
total features            1403
numeric features          1401
```

Frozen validation artifacts:

```text
reports/checkpoint_02/cv_folds.csv
reports/checkpoint_02/baseline_fold_scores.csv
reports/checkpoint_02/baseline_summary.csv
reports/checkpoint_02/train_prediction_diagnostics.csv
reports/checkpoint_02/checkpoint_02_report.md
```

## Validation finding that must carry forward

Two five-fold protocols are frozen:

- `fold_state_stratified` — primary.
- `fold_municipality_grouped` — spatial/generalization robustness.

The municipality-grouped protocol is much harder. Representative baseline RMSE:

```text
state-stratified ExtraTrees A7   0.4966
municipality-grouped Ridge A3    0.7426
municipality-grouped ExtraTrees A6 0.8835
```

Do not optimize only the easier state-stratified metric. Spatial/municipal generalization is a
first-class risk.

## Clean vs competition

Keep the two tracks explicit.

`clean`
: historical/static, intended for defensible prospective-style modeling.

`competition`
: clean plus rule-permitted 2025/full-season/contemporaneous public covariates.

The exact operational forecast cutoff is still unresolved. Do not describe competition-mode
features as a strict prospective forecast.

SIAP 2025 is a competition-only municipal outcome proxy and must stay explicitly labeled.

## Feature QC finding

Only one CHIRPS daily competition feature survives in Feature Table v1. Most candidate CHIRPS
daily aggregates were dropped as all-null by the builder. Investigate before claiming a rich
CHIRPS contribution.

Train-versus-prediction screening flagged only 6/1401 numeric features under the configured
descriptive shift/support thresholds. These X-only diagnostics are not labels and must not be
used as a hidden-test tuning target.

## Modeling rules for Checkpoint 03

Reuse `reports/checkpoint_02/cv_folds.csv`.

Candidate families may include CatBoost, Ridge/ElasticNet, ExtraTrees/RandomForest,
HistGradientBoosting and LightGBM/XGBoost if justified.

Because n=138 and p is large, keep searches disciplined.

Any learned preprocessing must be inside folds:

- imputation;
- scaling;
- PCA/PLS;
- supervised feature selection;
- encoding;
- target-aware transforms.

Keep fold-level RMSE/MAE/R² and machine-readable experiment records.

Do not regenerate folds because a result is inconvenient.

## Repository rules

Source evidence:

```text
data/source/
docs/official/
docs/reference/
```

must not be edited to fit code.

Stable reusable logic belongs under `src/geocebada/`.

Notebooks orchestrate exploration; they are not the canonical implementation.

Before adding a public helper, inspect `docs/FUNCTION_INDEX.md`.

For Streamlit/deployment work, read `app/AGENTS.md` first.

Large external raw/interim data remain gitignored. The deliberate exception is canonical
`data/processed/features_v1/`, which is versioned.

Material AI-assisted work must be recorded in `docs/AI_USAGE.md`.

## Known maintenance items

- pandas fragmentation warning in daily CHIRPS construction: performance issue, build still
  validated;
- h5py/HDF5 build/runtime mismatch warning on workstation: environment issue, WaPOR build still
  completed;
- operational prediction cutoff remains unresolved;
- CHIRPS daily feature survival needs QC.

## Immediate next action

Open Checkpoint 03 — Modeling. Compare controlled model families on the frozen Feature Table v1
and both frozen validation protocols, document every experiment, and only then freeze the final
pipeline for the 59 predictions.
